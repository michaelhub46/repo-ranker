# React App Environment Variables
# Backend API URL - change this if your backend runs on a different port
# Make sure this matches your backend server configuration
REACT_APP_API_URL=http://localhost:3001

# Development settings
# Set to false to reduce bundle size in production builds
# Set to true for easier debugging in development
GENERATE_SOURCEMAP=true

# Optional: API timeout in milliseconds
# REACT_APP_API_TIMEOUT=10000